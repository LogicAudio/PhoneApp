# Bugs — front page

Open defects live here as `NNN_snake_topic.md` (next sequential number); solved ones move to
[Solved/](Solved/). File one by telling the agent: `bug: <name + description>`. Hygiene (unique
numbers, template headings, this table) is machine-checked by `scripts/check_knowledge.py`.

## Open bugs

| # | Bug | Severity |
|---|-----|----------|
| — | _none_ | |

## Severity legend

- 🔴 breaks correctness (wrong data, lost entries, broken import/export)
- 🟡 wrong but bounded (bad display, recoverable state)
- 🔵 cosmetic / tech-debt
